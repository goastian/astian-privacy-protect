[package]
name = "midori_privacy_adblock_core"
version = "0.1.0"
authors = ["Astian, Inc."]
edition = "2021"
description = "Rust/WASM ad-blocking core for Midori Privacy."
license = "MPL-2.0"

[lib]
crate-type = ["cdylib"]

[profile.release]
# This makes the compiled code faster and smaller, but it makes compiling slower,
# so it's only enabled in release mode.
lto = true
codegen-units = 1
opt-level = "z"
panic = "abort"

[features]
# If you uncomment this line, it will enable `wee_alloc`:
#default = ["wee_alloc"]
default = ["fast-single-thread"]
fast-single-thread = ["adblock/single-thread"]

[dependencies]
# The `wasm-bindgen` crate provides the bare minimum functionality needed
# to interact with JavaScript.
adblock = { version = "0.12.5", default-features = false, features = [
  "embedded-domain-resolver",
  "full-regex-handling",
] }
serde_json = "1.0"
wasm-bindgen = "0.2.100"

# `wee_alloc` is a tiny allocator for wasm that is only ~1K in code size
# compared to the default allocator's ~10K. However, it is slower than the default
# allocator, so it's not enabled by default.
wee_alloc = { version = "0.4.2", optional = true }
console_error_panic_hook = "0.1.5"

# The `web-sys` crate allows you to interact with the various browser APIs,
# like the DOM.
[dependencies.web-sys]
version = "0.3.77"
features = ["console"]

# These crates are used for running unit tests.
[dev-dependencies]
wasm-bindgen-test = "0.3.50"
futures = "0.3.31"
js-sys = "0.3.77"
wasm-bindgen-futures = "0.4.50"
